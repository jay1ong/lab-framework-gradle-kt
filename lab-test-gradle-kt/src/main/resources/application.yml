server:
  port: 11006
spring:
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
  main:
    allow-bean-definition-overriding: true
lab:
  web:
    advice:
      exclude-packages: ["springfox.**","aaa"]
      enabled: true
axon:
  axonserver:
    enabled: false

